services:
  sitemap-generator:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # Persist captures between runs
      - ./captures:/app/captures
    environment:
      - NODE_ENV=production
    restart: unless-stopped
